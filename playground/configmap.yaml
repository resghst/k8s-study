apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # simple key/value pairs — good for env vars
  APP_COLOR: blue
  APP_MODE: production
  # a whole file — good for mounting as a config file
  app.conf: |
    color = blue
    mode = production
    retries = 3
